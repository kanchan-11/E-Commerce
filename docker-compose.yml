version: '3.8'

services:
  # SQL Server Database
  sqlserver:
    image: mcr.microsoft.com/mssql/server:2022-latest
    container_name: learning-sqlserver
    environment:
      - ACCEPT_EULA=Y
      - SA_PASSWORD=${DB_PASSWORD}
      - MSSQL_PID=Developer
    ports:
      - "1433:1433"
    volumes:
      - sqlserver-data:/var/opt/mssql
    networks:
      - learning-network
   

  # ASP.NET Core Application
  learningweb:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: learning-app
    environment:
      - ASPNETCORE_ENVIRONMENT=Production
      - ASPNETCORE_URLS=http://+:8080
      - ConnectionStrings__DefaultConnection=Server=sqlserver;Database=db_learning3;User Id=sa;Password=${DB_PASSWORD};TrustServerCertificate=True;MultipleActiveResultSets=true
      - Stripe__SecretKey=${STRIPE_SECRET_KEY}
      - Stripe__PublishableKey=${STRIPE_PUBLISHABLE_KEY}
      - SendGrid__SecretKey=${SENDGRID_SECRET_KEY}
    ports:
      - "5000:8080"
    depends_on:
      - sqlserver
    networks:
      - learning-network
    volumes:
      - /srv/ecommerce/uploads:/app/wwwroot/images/products
    restart: unless-stopped

volumes:
  sqlserver-data:
    driver: local

networks:
  learning-network:
    driver: bridge
